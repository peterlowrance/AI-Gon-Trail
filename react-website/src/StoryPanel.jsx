import UserInput from "./UserInput";


export default function StoryPanel(props) {
    // Will be the left side panel
    // Will have top section for scrollable story/scenarios
    // Will have user input at the bottom
    return <div>
        Story panel
        <UserInput />
    </div>
}